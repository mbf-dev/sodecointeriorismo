[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  # Directory for Netlify Functions (if needed in the future)
  directory = "netlify/functions"
