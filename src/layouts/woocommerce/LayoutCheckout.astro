---
import Layout from '../Layout.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <div class="container mx-auto py-12 px-4">
    <h1 class="text-3xl font-bold mb-8 text-primary">Finalizar Compra</h1>
    <div class="alert alert-success">
        <span>Checkout seguro en proceso de integración.</span>
    </div>
    <div class="mt-8">
        <slot />
    </div>
  </div>
</Layout>
